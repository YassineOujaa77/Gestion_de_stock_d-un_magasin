@model GestionDeStockMagasin.ViewModels.AchatIndexViewModel;
@{
    ViewData["Title"] = "index";
    
}

<h1>index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                Client
            </th>
            <th>
                Produit
            </th>
            <th>
                Qte
            </th>
            <th>
                Prix_total
            </th>
            <th>
                livrer
            </th>
            <th>
                CommmandId
            </th>
            <th></th>  
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.facturesTemp) {
        <tr>
            <td>
                @item.ClientId
            </td>
            <td>
               @item.ProductId
            </td>
            <td>
               @item.Qte
            </td>
            <td>
               @item.Prix_total
            </td>
            <td>
               @item.livrer
            </td>
            <td>
               @item.CommandId
            </td>
        </tr>
}
    </tbody>
</table>

<div class="w-75 btn-group" role="group">
                        <a asp-controller="Achat" asp-action="Generate" 
                        class="btn btn-primary mx-2"> <i class="bi bi-pencil-square"></i> Generée facture </a>

<div>
    <a asp-controller="Achat" asp-action="Index">Back to Achats</a>
</div>

